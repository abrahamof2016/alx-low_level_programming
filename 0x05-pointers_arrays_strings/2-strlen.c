#include <stdio.h>
#include "main.h"
#include <string.h>
/**
  * _strlen - return length of a string.
  * @s: a pointer argument.
  * Return: ingeger value.
  */
int _strlen(char *s)
{
	int a;

	a = strlen(s);
	return (a);
}
