#include <stdio.h>
#include "main.h"
#include <string.h>
/**
  * _strcmp - compare two strings.
  * @s1: a string.
  * @s2: a string.
  * Return: an integer.
  */
int _strcmp(char *s1, char *s2)
{
	int a = strcmp(s1, s2);
	return (a);
}
