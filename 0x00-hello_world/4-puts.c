#include <stdio.h>
/**
  * main - Entry point
  * Return: Always 0 (success)
  */
int main(void)
{
	char *str;

	str = "Programming is like building a multilingual puzzle";
	putchar(34);
	puts(str);
	return (0);
}
