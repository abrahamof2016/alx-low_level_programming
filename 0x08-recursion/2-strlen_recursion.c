#include <stdio.h>
#include "main.h"
#include <string.h>
/**
  * _strlen_recursion - finds the length of a string.
  * @s: string.
  * Return: an integer.
  */
int _strlen_recursion(char *s)
{
	int l;

	l = strlen(s);
	return (l);
}
