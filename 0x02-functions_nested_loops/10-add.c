#include <stdio.h>
#include "main.h"
/**
  * add - a function which adds two integers and returns the result.
  * @a: an argument of type int.
  * @b: an argument of type int.
  * Return: integer value.
  */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
