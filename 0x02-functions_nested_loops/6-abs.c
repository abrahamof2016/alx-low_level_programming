#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
  * _abs - a function that computes the absolute value of an integer.
  * @a: an argument of type int.
  * Return: value
  */
int _abs(int a)
{
	int b;

	b = abs(a);
	return (b);
}

